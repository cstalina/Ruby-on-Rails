<h1>Maftei & Costache's BLOG</h1>
<br/>


<ul>
<% @articles.each do |article| %>
	<li>
		<div>

  			<h3 > <%= link_to article.title, "/articles/#{article.id}" %></h3> 
    		<p> 

    		<%= article.created_at.strftime("%B %d, %Y") %> 
    		</p>

    		
		</div>
	</li>
<% end %>
</ul>

<br/>
<%= link_to "Back", "/articles" %>